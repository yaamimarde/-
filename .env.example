# 复制为 .env 并填入真实值后执行 docker compose up -d --build
# 注意：.env 已被 gitignore，不会提交到仓库

MYSQL_ROOT_PASSWORD=your_password
MYSQL_DATABASE=pharmaceutical
JWT_SECRET=change-me-to-a-secure-random-key-at-least-32-chars

# 首次启动或空库时自动写入演示数据（供应商、药品、批次、流水）
SEED_DEMO_DATA=true
