@if (IsLoading)
{
    <div class="loading-state">
        <div class="spinner"></div>
        <span>@Text</span>
    </div>
}

@code {
    [Parameter] public bool IsLoading { get; set; }
    [Parameter] public string Text { get; set; } = "加载中...";
}
